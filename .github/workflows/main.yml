name: Test
on:
  push:
jobs:
  Test:
    name: Test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test
        run: |
          curl https://altstore.io/adps/altstore/2_1_1/variant/8ad0efba-9dcc-4f62-bd30-25cffd4596b4.ipa > app.ipa
          dist_package_tool validate app.ipa -v
          dist_package_tool --help
